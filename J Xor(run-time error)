
def boole(x,y):
  if (x,y).count(1)==1:
    return True
  else:
    return False
  
  
numoftimes=int(input())
if numoftimes>=1 and numoftimes<=1000:
  for i in range(numoftimes):
    x,y=list(map(int,input().split(' ')))
    if boole(x,y)==True:
      print(1)
    else:
      print(0)
